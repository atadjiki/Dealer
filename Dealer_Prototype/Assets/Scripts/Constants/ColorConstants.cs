namespace Constants
{
    public class ColorConstants
    {
        public enum SkinType { Pink, Pale, Fair, Tan, Dark };

        public enum HairType { Blonde, Brunette, Black, Giner };

        public enum ClothingType { Black, Grey, Blue, Red, White, Olive };
    }
}
