namespace Constants
{
    public class State
    {
        public enum GameMode { MainMenu, GamePlay, Loading };
        public enum GamePlayMode { PreDay, Day, PostDay, Conversation, RandomEvent, Paused };
        public enum TimeMode { Slow, Paused, Normal, Fast, VeryFast };

    }
}

